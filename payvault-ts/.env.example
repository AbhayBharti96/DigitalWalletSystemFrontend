# Copy to .env and adjust for your environment.
# Never commit real secrets; Razorpay key_id is public, secret stays on the server.

VITE_API_BASE_URL=http://localhost:8080
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
